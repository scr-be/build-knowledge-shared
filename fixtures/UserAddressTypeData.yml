---
UserAddressType:

    version:

        structure: 1.0.0
        data:      1.0.0

    orm:

        priority:   200
        mode:       truncate
        entity:     s.security.user_address_type.entity.class
        repository: s.security.user_address_type.repo

    dependencies: null

    data:

        1:
            slug: 'home'
            name: 'Home'

        2:
            slug: 'work'
            name: 'Work'

        3:
            slug: 'po'
            name: 'P.O. Box'

        4:
            slug: 'other'
            name: 'Other'
